<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruyi.teach.mapper.AiResourceLiteMapper">

    <select id="selectActiveByTeacherId" resultType="com.ruyi.teach.model.entity.AiResourceLite">
        select
            id,
            teacher_id,
            type,
            title,
            content,
            params_json,
            is_published,
            is_delete,
            create_time,
            update_time
        from ai_resource
        where teacher_id = #{teacherId}
          and is_delete = 0
        order by update_time desc, id desc
    </select>

    <select id="selectActiveByIds" resultType="com.ruyi.teach.model.entity.AiResourceLite">
        select
            id,
            teacher_id,
            type,
            title,
            content,
            params_json,
            is_published,
            is_delete,
            create_time,
            update_time
        from ai_resource
        where is_delete = 0
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        order by update_time desc, id desc
    </select>

</mapper>
